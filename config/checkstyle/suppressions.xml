<?xml version="1.0"?>
<!DOCTYPE suppressions PUBLIC
    "-//Checkstyle//DTD SuppressionFilter Configuration 1.2//EN"
    "https://checkstyle.org/dtds/suppressions_1_2.dtd">
<suppressions>
    <!-- Add project-specific suppressions here, each with a comment explaining why.
         Example (constructor injection exceeding the 3-parameter limit):
         <suppress checks="ParameterNumber"
                   files="[\\/]domain[\\/]service[\\/].*ServiceImpl\.java$"/> -->

    <!-- Internal fan-in constructor bundling the catalogue kind + its data; the public API is the
         one-argument static factories, so the parameter count is not an API concern. -->
    <suppress checks="ParameterNumber"
              files="[\\/]contributor[\\/]PropertyDefaultsContributor\.java$"/>
</suppressions>
